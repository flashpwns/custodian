{
  "name": "custodian",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent, canon-aware narrative simulation framework.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node --test tests/artifact-validation.test.js tests/canonical-kernel.test.js tests/perception-projection.test.js",
    "demo": "node runtime/demo.js",
    "replay": "node runtime/demo.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
