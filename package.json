{
  "name": "custodian",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent, canon-aware narrative simulation framework.",
  "scripts": {
    "test": "node --test tests/contract-fixtures.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
