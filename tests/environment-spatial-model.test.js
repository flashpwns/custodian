const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");const {replayEnvironment}=require("../runtime/reference/environment.js");const p=JSON.parse(fs.readFileSync(path.join(__dirname,"../examples/environment-spatial/plan.json"),"utf8"));const a=replayEnvironment(structuredClone(p)),b=replayEnvironment(structuredClone(p));assert.deepEqual(a,b,"movement replay is deterministic");assert.equal(a.positions["agent-a"],"room-b","only traversable route moves position");assert.equal(a.history[1].status,"blocked","adjacency does not imply traversability");assert.equal(a.containment.tool,"backpack","nested containment remains explicit");console.log("validated environment spatial model");
